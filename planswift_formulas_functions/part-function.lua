if '[Supplier]' = 'Knauf' then result := '[Wall Type 1200 (knauf)]'
else 
if '[Supplier]' = 'CSR' then result := '[Wall Type 1200 (csr)]'
else
if '[Supplier]' = 'ADX' then result := '[Wall Type 1200 (adx)]'
else result := 'unknown supplier'

    if '[Supplier]' = 'Knauf' then result := '[Wall Type 1350 (knauf)]'
else 
if '[Supplier]' = 'CSR' then result := '[Wall Type 1350 (csr)]'
else
if '[Supplier]' = 'ADX' then result := '[Wall Type 1350 (adx)]'
else result := 'unknown supplier'

    Wall Type 1350 (adx)
    Wall Type 1350 (csr)
    Wall Type 1350 (Knauf) 

    if '[Supplier]' = 'Knauf' then result := '[Wall Type 1200 (knauf)]'
else 
if '[Supplier]' = 'CSR' then result := '[Wall Type 1200 (csr)]'
else
if '[Supplier]' = 'ADX' then result := '[Wall Type 1200 (adx)]'
else 
if '[Supplier]' = 'Knauf' then result := '[Wall Type 1350 (knauf)]'
else 
if '[Supplier]' = 'CSR' then result := '[Wall Type 1350 (csr)]'
else
if '[Supplier]' = 'ADX' then result := '[Wall Type 1350 (adx)]'
else result := 'unknown supplier'